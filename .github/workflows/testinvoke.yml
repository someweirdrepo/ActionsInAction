on:
  repository_dispatch:
    types: [event1]  # Must match the event_type you send

jobs:
  handle-dispatch:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo "Received dispatch: ${{ github.event.client_payload.message }}"